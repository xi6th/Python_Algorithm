new_list = [1,2,3]
result = new_list[0] # accesing the values of an array using the index
print(result)

#Searching in arrays
if 1 in new_list:
    print(True)
    

# Appending 
numbers = []
numbers.extend([45,2,4])
print(numbers)

# Deleting
x = numbers.pop()
print(x)